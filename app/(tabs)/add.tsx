import { View } from 'react-native';

// File ini adalah placeholder untuk tombol tab tengah.
// Expo Router memerlukan file ini ada, tetapi kita tidak perlu me-render apa pun
// karena aksinya (membuka modal) sudah ditangani di _layout.tsx.
export default function AddTabPlaceholder() {
  return <View />;
}